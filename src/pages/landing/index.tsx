import React from "react";

const Landing: React.FC = () => {
    return (
        <main>
           <div>salut voici la page principal de notre sit web</div>
        </main>
    );
};

export default Landing;